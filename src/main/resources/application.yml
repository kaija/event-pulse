# Kafka Configuration
# Note: Use kafka:9092 when running Flink in Docker, localhost:9092 from host
kafka:
  bootstrap-servers: kafka:9092
  topic: user-tracking-events
  consumer-group-id: flink-event-trigger

# Profile API Configuration
# For testing, use the Mock Profile API Server:
#   Start with: ./start-mock-api.sh 8080
#   See MOCK_API.md for details
# Note: Use host.docker.internal when running Flink in Docker
profile-api:
  base-url: http://host.docker.internal:8080
  timeout-ms: 5000
  retry-attempts: 3

# Filter Configuration
filter:
  script-path: ./scripts/filter.av

# Actions Configuration
actions:
  webhook:
    url: http://localhost:8080/webhook
    timeout-ms: 3000
  debug:
    enabled: true

# Flink Configuration
flink:
  parallelism: 2
  checkpoint-interval-ms: 60000
  state-backend: rocksdb
  state-ttl-minutes: 10
